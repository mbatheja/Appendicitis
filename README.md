To run front end code download front end folder as is
In command prompt window set directory to front end folder
Next run: streamlit run login+form.py

Open another command prompt window run the code in 
FLASK COMMAND.txt (please make sure you have flask installed)

Now enter credentials: use physician usernames from credentials file 
and general password from hashed_passwords.ipynb and login

#running database
Open pgadmin4, setup database named "DSS"
In command prompt run full_backup.sql
